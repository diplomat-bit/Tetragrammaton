const fs = require('fs');

const banksJson = {
  // I will just read the prompt input indirectly or just copy-paste it...
  // Wait, I have the prompt in my context! I can just write a script to inject it.
};
